1. Process .mov file with MPEG Streamclip and save as "s" so that the image
files will be saved as "s 001.png", "s 002.png", etc.
2. Run: python -m SimpleHTTPServer
  Then load localhost:8000/view.html
